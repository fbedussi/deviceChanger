//var devices = ['smartphone', 'tablet', 'desktop'];
//chrome.browserAction.onClicked.addListener(function(tab) {
//    if (!active) {
//        active = true;
//        chrome.tabs.executeScript(null, {file: "content_script.js"});
//        chrome.tabs.insertCSS(null, {file: "style.css"});            
//    } else {
//        active = false;
//        chrome.tabs.executeScript(null, {code: "document.body.removeChild(document.querySelector('#fbAdsContainer'));[].forEach.call(document.querySelectorAll('.mark'), function(el){el.classList.remove('mark')});"});
//    }
//});
